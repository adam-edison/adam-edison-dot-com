/**
 * Constants for form validation operations.
 * Contains validation limits and constraints for form fields.
 */
export class FormValidationConstants {
  /**
   * Minimum length required for first and last name fields
   */
  static readonly NAME_MIN_LENGTH = 2;

  /**
   * Maximum length allowed for first and last name fields
   */
  static readonly NAME_MAX_LENGTH = 50;
}
